{
  "name": "@calculator/common",
  "version": "1.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.4",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "build": "yarn clean && tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist"
  }
}
