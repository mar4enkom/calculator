import {Digits} from "@calculator/common";

export function getDigitColumnItems(): string[] {
    return [
        Digits.ONE,
        Digits.TWO,
        Digits.THREE,
        Digits.FOUR,
        Digits.FIVE,
        Digits.SIX,
        Digits.SEVEN,
        Digits.EIGHT,
        Digits.NINE,
        Digits.ZERO,
    ];
}