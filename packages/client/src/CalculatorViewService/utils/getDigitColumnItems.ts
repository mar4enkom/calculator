import {Digits} from "userConfig/constants/constants";

export function getDigitColumnItems(): string[] {
    return [
        Digits.ONE,
        Digits.TWO,
        Digits.THREE,
        Digits.FOUR,
        Digits.FIVE,
        Digits.SIX,
        Digits.SEVEN,
        Digits.EIGHT,
        Digits.NINE,
        Digits.ZERO,
    ];
}